{
  "name": "conduit-angular",
  "version": "2.0.0",
  "description": "Angular 21 frontend for Conduit (RealWorld). The official app lives in app-ng/; the original AngularJS app is archived under legacy/.",
  "author": "Thinkster",
  "license": "MIT",
  "scripts": {
    "build": "cd app-ng && npm ci && npm run build",
    "serve": "serve -s app-ng/dist/app-ng/browser -l 4100 --no-clipboard",
    "test:unit": "cd app-ng && CI=true npm test",
    "test:contract": "jest tests/contract/ --testTimeout=15000",
    "pretest:e2e": "npm run build",
    "test:e2e": "npx playwright test",
    "test:integration": "bash scripts/run-integration.sh"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "jest": "^27.5.1",
    "js-yaml": "^3.14.2",
    "serve": "^14.2.6"
  }
}
